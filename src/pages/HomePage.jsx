import Navbar from '../components/Navbar';
import Hero from '../components/Hero';

const HomePage = () => {
  return (
    <header>
      <Navbar/>
      <Hero />
    </header>
  );
};

export default HomePage;
